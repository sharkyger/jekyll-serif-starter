{
  "name": "jekyll-serif-starter",
  "version": "0.1.0",
  "description": "Dev tooling (SCSS linting) for the Jekyll Serif Starter. The site itself is built with Jekyll/Bundler — see Gemfile.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "lint:css": "stylelint \"_sass/components/**/*.scss\" \"_sass/pages/**/*.scss\""
  },
  "devDependencies": {
    "stylelint": "17.12.0",
    "stylelint-config-standard-scss": "17.0.0"
  }
}
